# Data

Only reproducible synthetic data is used in the public project.

Generated datasets belong in `data/generated/` and are ignored by Git. The simulator configuration, random seed, schemas, and planted-fault definitions remain version controlled so results can be reproduced without committing large files.

